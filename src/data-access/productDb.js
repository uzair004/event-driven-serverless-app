/* eslint-disable no-console */
'use strict';

// const { makeTs, head } = require('../util/util');

// const { productDM } = require('../model/dataModel');

// TODO: not implemented...

function makeProductDb(/*{ makeDb, makeDbConnect, getTableName }*/) {
  return Object.freeze({});
}

module.exports = {
  makeProductDb,
};
